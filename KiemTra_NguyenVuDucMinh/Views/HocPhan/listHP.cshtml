@model IEnumerable<KiemTra_NguyenVuDucMinh.Models.HocPhan>

@{
    ViewBag.Title = "listHP";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>DANH SÁCH HỌC PHẦN</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            Tên Học Phần
        </th>
        <th>
            Số Tín chỉ
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TenHP)
        </td>
        <td>fff
            @Html.DisplayFor(modelItem => item.SoTinChi)
        </td>
        <td>
            @Html.ActionLink("Đăng Ký", "DangKy", "TaiKhoan", new { id=item.MaHP })          
        </td>
    </tr>
}
</table>
